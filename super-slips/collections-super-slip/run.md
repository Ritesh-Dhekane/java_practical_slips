# Collections Super Slip - How to Run

## Compile
```bash
javac Main.java
```

## Run
```bash
java Main
```

## What to observe
- list sorting with `Collections.sort(...)`
- iterator usage with `Vector`
- duplicate removal with `HashSet`
- automatic sorting with `TreeSet`
- key-value traversal with `HashMap`
