# Slip 27 – Q1: JSP Implicit Objects – Employee Details
## What the program does
Sets employee attributes in `session`, company info in `application`. Displays all using `request`, `session`, and `application` implicit objects.
## Syllabus Unit
**UNIT 4** – JSP
