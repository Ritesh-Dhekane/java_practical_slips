# Slip 29 – Q1: Manager extends Employee with Bonus
## What the program does
`Manager extends Employee` with additional `bonus` attribute. `display()` calls `super.display()` then shows bonus and total package.
## Syllabus Unit
**UNIT 1** – Inheritance
