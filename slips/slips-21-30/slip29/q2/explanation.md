# Slip 29 – Q2: Servlet – Store/Retrieve Username Cookie
## What the program does
Servlet stores a username in a cookie (`addCookie()`). On the next request, it retrieves the cookie (`getCookies()`) and displays the stored username.
## Syllabus Unit
**UNIT 3** – Servlets
