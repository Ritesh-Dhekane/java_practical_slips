# Slip 25 – Q2: Servlet Cookies
## What the program does
Servlet that sets cookies (`username`, `role`) via `response.addCookie()` and retrieves them via `request.getCookies()` in a subsequent request.
## Syllabus Unit
**UNIT 3** – Servlets
